# {{title}}

Operation id: `{{operationId}}`

Category: `{{categoryId}}`

{{summary}}

Use this operation through ModDevMCP request APIs. Do not replace it with shell-driven keyboard input, mouse movement, or other OS-level automation.
For discovery and baseUrl selection, follow `moddev-usage` first, then run this operation.

Target side rule:

{{targetSideRule}}
`targetSide` is required only when both eligible sides are live.

Request example:

{{curlExample}}

Common failure codes:

{{commonFailureCodes}}
